// import { selectPublisherListingDomain } from '../selectors';

describe('selectPublisherListingDomain', () => {
  it('Expect to have unit tests specified', () => {
    expect(true).toEqual(false);
  });
});
